pipeline{
    agent any
    stages{
        stage("Build"){
	           steps {
                       dir('coit-frontend/Dockerfile-multistage'){
		                   sh 'cd coit-frontend && npm install && npm run build'
                      }
                   }
               }
            }
        }
